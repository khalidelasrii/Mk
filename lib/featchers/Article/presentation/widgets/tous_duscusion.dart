import 'package:flutter/material.dart';

class TousDescusion extends StatelessWidget {
  const TousDescusion({super.key, required this.userid});
  final String userid;

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
