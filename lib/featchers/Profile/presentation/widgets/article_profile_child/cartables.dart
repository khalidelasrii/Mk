import 'package:flutter/material.dart';

class Cartables extends StatelessWidget {
  const Cartables({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
